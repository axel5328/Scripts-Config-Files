http:
  routers:

    jellyfin:
      rule: "Host(`jellyfin.node-forge.eu`)"
      entryPoints:
        - websecure
      tls:
        certResolver: cloudflare
      service: jellyfin
      middlewares:
        - crowdsec

  services:

    jellyfin:
      loadBalancer:
        servers:
          - url: "http://192.168.178.69:8096"
