services:
  grafana-matrix-forwarder:
    image: vincejv/grafana-matrix-forwarder:latest
    container_name: grafana-matrix-forwarder
    restart: unless-stopped
    env_file:
      - .env
    networks:
      - monitoring

networks:
  monitoring:
    external: true
    name: monitoring_default
